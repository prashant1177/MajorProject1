<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>Listing Details</h2>
    <ul>
        <li><%= listing.title %></li>
        <li><%= listing.description %></li>
        <li><%= listing.price.toLocaleString("en-IN")%>/per KM</li>
        <li><%= listing.location %></li>
        <li><%= listing.capacity %> peoples</li>
    </ul>
    <a href="/listings/<%= listing._id %>/edit">Edit Listing</a>
    <form action="/listings/<%= listing._id %>?_method=DELETE" method="post"><button>Remove Listing</button></form>
</body>
</html>